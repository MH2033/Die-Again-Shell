#include <csignal>
#include <iostream>
#include <climits>
#include <stdio.h>
#include <stdlib.h>

using namespace std;
void signalHandler(int sigNum){
    // cin.ignore(std::numeric_limits<std::streamsize>::max());
    // cin.clear();
    // std::cin.ignore(INT_MAX);
    // fflush(stdin);
    // getchar();
}